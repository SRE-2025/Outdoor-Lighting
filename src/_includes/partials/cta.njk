{% import "partials/icons.njk" as ico %}
<section class="section">
  <div class="wrap">
    <div class="cta-band">
      <h2>{{ ctaHeading if ctaHeading else "Let's light up your property." }}</h2>
      <p>{{ ctaText if ctaText else "Free, no-pressure consultations across Buda, Kyle, San Marcos and all of Hays County. Talk to a real local team, not a call center." }}</p>
      <a class="btn btn--white btn--lg" href="/contact/">Get a Free Consultation</a>
      <a class="btn btn--ghost btn--lg" href="tel:{{ site.phoneRaw }}" style="color:#fff;border-color:rgba(255,255,255,.4)">{{ ico.svg("phone") }} {{ site.phone }}</a>
    </div>
  </div>
</section>
